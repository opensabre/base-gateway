<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>io.github.opensabre</groupId>
    <artifactId>base-gateway</artifactId>
    <version>1.1.4-SNAPSHOT</version>
    <packaging>jar</packaging>

    <name>base-gateway</name>
    <description>Gateway Project for Opensabre</description>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
        <opensabre.version>1.1.4-SNAPSHOT</opensabre.version>
        <lombok.version>1.18.42</lombok.version>
        <spring-boot-maven-plugin.version>4.0.7</spring-boot-maven-plugin.version>
        <jib-maven-plugin.version>3.4.6</jib-maven-plugin.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>io.github.opensabre</groupId>
                <artifactId>opensabre-base-dependencies</artifactId>
                <version>${opensabre.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <!--api网关-->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-gateway-server-webflux</artifactId>
        </dependency>
        <!-- API 级熔断由 Spring Cloud Gateway 官方 reactive Resilience4j 适配器承载。 -->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-circuitbreaker-reactor-resilience4j</artifactId>
        </dependency>
        <!--WebFlux基础组件-->
        <dependency>
            <groupId>io.github.opensabre</groupId>
            <artifactId>opensabre-starter-webflux</artifactId>
        </dependency>
        <!--统一使用 OpenSabre 注册中心和配置中心 starter。-->
        <dependency>
            <groupId>io.github.opensabre</groupId>
            <artifactId>opensabre-starter-register</artifactId>
            <exclusions>
                <!-- The gateway loads Nacos configuration through
                     spring.config.import. Keeping the legacy Bootstrap starter
                     would load base-gateway.yml twice and let the stale
                     Bootstrap property source shadow hot-refreshed values. -->
                <exclusion>
                    <groupId>org.springframework.cloud</groupId>
                    <artifactId>spring-cloud-starter-bootstrap</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>io.github.opensabre</groupId>
            <artifactId>opensabre-starter-config</artifactId>
            <exclusions>
                <!-- Use Spring Boot Config Data below; the legacy bootstrap
                     manager is created before Nacos client context options
                     can be bound. -->
                <exclusion>
                    <groupId>org.springframework.cloud</groupId>
                    <artifactId>spring-cloud-starter-bootstrap</artifactId>
                </exclusion>
            </exclusions>
        </dependency>
        <dependency>
            <groupId>io.github.opensabre</groupId>
            <artifactId>opensabre-starter-security</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.opensabre</groupId>
            <artifactId>opensabre-starter-monitoring</artifactId>
        </dependency>
        <!--redis限流-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis-reactive</artifactId>
        </dependency>
        <!--共享WebFlux Session，用于网关多节点部署-->
        <dependency>
            <groupId>org.springframework.session</groupId>
            <artifactId>spring-session-data-redis</artifactId>
        </dependency>
        <!--缓存-->
        <dependency>
            <groupId>com.github.ben-manes.caffeine</groupId>
            <artifactId>caffeine</artifactId>
            <version>3.1.8</version>
        </dependency>
        <!-- Sentinel 暂停：待 Native/Java 25 兼容性专项验证后再按需恢复。 -->
        <!--负载均衡-->
        <dependency>
            <groupId>org.springframework.cloud</groupId>
            <artifactId>spring-cloud-starter-loadbalancer</artifactId>
        </dependency>
        <!--授权认证-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-client</artifactId>
        </dependency>
        <!-- 解密控制面发布到 Nacos 的 OAuth2 Client 密钥。 -->
        <dependency>
            <groupId>com.github.ulisesbocchio</groupId>
            <artifactId>jasypt-spring-boot-starter</artifactId>
        </dependency>
        <!--资源服务器-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-oauth2-resource-server</artifactId>
        </dependency>
        <dependency>
            <groupId>io.github.opensabre</groupId>
            <artifactId>opensabre-test</artifactId>
        </dependency>
        <!--测试基础依赖-->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!--显式启用Lombok注解处理，兼容新版JDK默认不扫描classpath processor的行为-->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
            <!--springboot官方构建插件，可构建docker image-->
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot-maven-plugin.version}</version>
                <executions>
                    <execution>
                        <goals>
                            <goal>build-info</goal>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.2</version>
                <configuration>
                    <systemPropertyVariables>
                        <JM.LOG.PATH>${project.build.directory}/logs</JM.LOG.PATH>
                    </systemPropertyVariables>
                </configuration>
            </plugin>
            <!-- Jib Maven插件 -->
            <plugin>
                <groupId>com.google.cloud.tools</groupId>
                <artifactId>jib-maven-plugin</artifactId>
                <version>${jib-maven-plugin.version}</version>
                <dependencies>
                    <!-- 添加所有权扩展 -->
                    <dependency>
                        <groupId>com.google.cloud.tools</groupId>
                        <artifactId>jib-ownership-extension-maven</artifactId>
                        <version>0.1.0</version>
                    </dependency>
                </dependencies>
                <configuration>
                    <pluginExtensions>
                        <pluginExtension>
                            <implementation>com.google.cloud.tools.jib.maven.extension.ownership.JibOwnershipExtension</implementation>
                            <configuration implementation="com.google.cloud.tools.jib.maven.extension.ownership.Configuration">
                                <rules>
                                    <rule>
                                        <glob>/app/logs/**</glob>
                                        <ownership>1001</ownership>
                                    </rule>
                                </rules>
                            </configuration>
                        </pluginExtension>
                    </pluginExtensions>
                    <!-- 基础镜像 -->
                    <from>
                        <image>eclipse-temurin:25-jre-alpine</image>
                    </from>
                    <!-- 目标镜像 -->
                    <to>
                        <image>${REGISTRY_URL}/${project.artifactId}:${project.version}</image>
                        <auth>
                            <username>${secret.id}</username>
                            <password>${secret.key}</password>
                        </auth>
                    </to>
                    <!-- 容器配置 -->
                    <container>
                        <user>1001</user>
                        <appRoot>/app</appRoot>
                        <workingDirectory>/app</workingDirectory>
                        <volumes>
                            <volume>/app/logs</volume>
                        </volumes>
                        <!-- 设置环境变量 -->
                        <environment>
                            <!-- 设置时区 -->
                            <TZ>Asia/Shanghai</TZ>
                        </environment>
                        <jvmFlags>
                            <jvmFlag>-XX:+UseContainerSupport</jvmFlag>
                            <jvmFlag>-XX:InitialRAMPercentage=10</jvmFlag>
                            <jvmFlag>-XX:MaxRAMPercentage=60</jvmFlag>
                            <jvmFlag>-Xss256k</jvmFlag>
                            <jvmFlag>-XX:MaxMetaspaceSize=128m</jvmFlag>
                            <jvmFlag>-XX:MaxDirectMemorySize=128m</jvmFlag>
                            <jvmFlag>-XX:+ExitOnOutOfMemoryError</jvmFlag>
                            <jvmFlag>-Dnacos.logging.default.config.enabled=false</jvmFlag>
                        </jvmFlags>
                        <creationTime>USE_CURRENT_TIMESTAMP</creationTime>
                    </container>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
